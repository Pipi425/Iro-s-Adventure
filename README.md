You first extract the Graphics, enemies, NPC/item/animal and sounds zip file. 

Put everything inside the unziped file as same level as those .py files. 

(For example, after you extract a file, the contents are:
unziped file -> the "files"
I want you put the "files" as same level as the .py files not the unziped file)

Finally extract everything in the five music zip file and put the mp3s in the musics file that you unziped from Sounds, including snowydark.mp3 of course.

Run the game through Main.py
